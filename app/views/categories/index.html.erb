<h1>Listing All Categories</h1>

<ul>
    
    <% @categories.each do |category| %>
        <li><%= link_to category.name, category_path(category) %> - <%= pluralize(category.articles.count, "article") %></li>
    <% end %>

</ul>

<%= will_paginate %>